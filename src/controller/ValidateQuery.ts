
type LOGIC = "AND" | "OR";
type MCOMPARATOR = "LT" | "GT" | "EQ";

// BEGIN BODY VALIDITY CHECK
// Checks body of query input
function isBodyValid(query: JSON) {
	return;
}

// Checks LOGICCOMPARISON
function isLogicComparisonValid(query: object) {
	return;
}

// Checks MCOMPARISON
function isMComparisonValid(query: object) {
	return;
}

// Checks SCOMPARISON
function isSComparisonValid(query: object) {
	return;
}

// Checks NEGATION
function isNegationValid(query: object) {
	return;
}
// END BODY VALIDITY CHECK


// BEGIN OPTIONS VALIDITY CHECK
function isOptionsValid(query: JSON) {
	return;
}

function isColumnsValid(query: object) {
	return;
}

function isOrderValid(query: object) {
	return;
}
// END OPTIONS VALIDITY CHECK
